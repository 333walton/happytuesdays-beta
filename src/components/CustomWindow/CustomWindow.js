// This file serves as a wrapper or customization layer around the core CustomWindow component
// It allows for further customization specific to certain usages if needed

// Import the core CustomWindow component from tools
import CustomWindow from '../tools/CustomWindow';
import './_styles.scss';

// Export it directly for now
// In the future, you could add customization or extended functionality here
export default CustomWindow;