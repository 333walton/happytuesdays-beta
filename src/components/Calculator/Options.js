/* unfortunately not functional at the time, just for show */

import React from "react";

const Options = () => {
  return (
    <div className="calculator-options">
      {/* Optional content like checkboxes/toggles */}
      <span>Standard</span>
    </div>
  );
};

export default Options;
